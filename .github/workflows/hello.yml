name: Hello # ワークフロー名
on: push # イベント（プッシュ時に起動）
jobs: # ジョブの定義
  hello: # ジョブのID
    runs-on: ubuntu-latest # ランナー（ubuntuで実行)
    steps:
      - run : echo "Hello, world" # シェルコマンド呼び出し
      - uses: actions/checkout@v4 # アクションの呼び出し
